import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  };
  module.exports = {
    // allowedDevOrigins: ["unstandardised-unforcing-errol.ngrok-free.dev"],
    allowedDevOrigins: ["10.244.37.151"],
  };
export default nextConfig;
